// This file is the main entry point for bundling the language server. It has only a side-effect import of the
// @actions/languageserver module, so that we can more easily reference it in the webpack configuration.
import "@actions/languageserver";
